import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F97C5A',
      light: '#F9F3F1',
      dark: '#303AF2',
    },
    secondary: {
      main: '#777DF2',
      light: '#A0A4F2',
    },
  },
});

export default theme;
